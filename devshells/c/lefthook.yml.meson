# lefthook.yml

pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{c,h}"
      run: just format {staged_files} && git add {staged_files}

    lint:
      glob: "*.{c,h}"
      run: just lint {staged_files}

pre-push:
  parallel: false
  commands:
    test:
      run: just test
