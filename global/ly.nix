{
  services.displayManager.ly = {
    enable = true;

    settings = {
      animation = "matrix";
      # auto_login_service = "ly-autologin";
      # ly_log = "/var/log/ly.log";
      #
      # bigClock = true;
      # asterik = "";
      # vi_mode = true;
      # x_vt = null;
    };
  };
}
